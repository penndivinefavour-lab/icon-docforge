(function () {
  const id='pdf-merge'; const {PDFDocument}=window.PDFLib;
  function render(){const w=document.createElement('div');w.innerHTML=`<div class="tool-section"><h3>Select PDFs</h3><div class="drop-zone" id="drop"><div class="drop-zone-icon">📄</div><div class="drop-zone-text">Tap to select two or more PDFs</div><input type="file" id="files" accept="application/pdf" multiple style="display:none"></div><p id="count" class="tool-desc">0 PDFs selected</p></div><button class="btn btn-primary btn-block" id="go" disabled>Merge PDFs</button><div id="result" style="margin-top:16px"></div>`;return w;}
  async function onMount(root){let files=[];const input=root.querySelector('#files'),go=root.querySelector('#go');input.onchange=()=>{files=Array.from(input.files);root.querySelector('#count').textContent=`${files.length} PDF(s) selected`;go.disabled=files.length<2};root.querySelector('#drop').onclick=()=>input.click();go.onclick=async()=>{go.disabled=true;go.textContent='Merging…';try{const out=await PDFDocument.create();for(const f of files){const src=await PDFDocument.load(await f.arrayBuffer());const pages=await out.copyPages(src,src.getPageIndices());pages.forEach(p=>out.addPage(p));}const saved=DFRuntime.saveBytes(await out.save(),'merged.pdf','application/pdf');DFRuntime.resultView(root,saved.name,files.length+' documents',()=>AndroidBridge.openFile(saved.uri,saved.mime),()=>AndroidBridge.shareFile(saved.uri,saved.mime));window.App.toast('PDFs merged','success');}catch(e){window.App.toast(e.message,'error');}finally{go.disabled=false;go.textContent='Merge PDFs';}};}
  window.DFRegistry=window.DFRegistry||{};window.DFRegistry[id]={id,name:'PDF Merge',render,onMount};
})();
