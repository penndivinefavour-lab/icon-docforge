# Byte-compiled / cache
__pycache__/
*.pyc
*.pyo

# Output artifacts (regenerated by the engine, not source)
output/

# Test results (regenerated each run)
tests/test_results.json

# Editor / OS
.DS_Store
*.swp

# Android build artifacts
android-apk/node_modules/
node_modules/
android-apk/android/.gradle/
android-apk/android/app/build/

# Bundled runtimes / heavy prebuilt binaries (large, not in git)
engines-bin/
